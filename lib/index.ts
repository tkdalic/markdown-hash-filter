import { markdownHashFilter } from "./markdown-hash-filter";
import { getFilenameAndContent } from "./get-filename-and-content";

module.exports = {
  markdownFilter: markdownHashFilter,
  getFilenameAndContent: getFilenameAndContent
}